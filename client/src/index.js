import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
//recuerda instalar boostrap con npm i boostrap
//aqui poner el boosttrap(el import ese tiene que ir aca)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
